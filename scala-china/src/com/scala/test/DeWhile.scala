package com.scala.test

/**
 * @author John
 */
object DeWhile {
  
  
  def main(args: Array[String]) : Unit = {
//    if(args.isEmpty)
    var line = ""
    do{
      line = readLine()
      println("Read:" + line)
    }while(line != "")
  } 
  
  println("fdasfds")
}